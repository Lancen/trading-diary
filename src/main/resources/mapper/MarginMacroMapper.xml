<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tradingdiary.mapper.MarginMacroMapper">

    <select id="selectDistinctTradeDates" resultType="java.time.LocalDate">
        SELECT DISTINCT trade_date FROM margin_macro WHERE trade_date BETWEEN #{start} AND #{end} AND exchange = #{exchange} ORDER BY trade_date
    </select>

    <select id="selectMaxTradeDate" resultType="java.time.LocalDate">
        SELECT MAX(trade_date) FROM margin_macro WHERE exchange = #{exchange}
    </select>

</mapper>
