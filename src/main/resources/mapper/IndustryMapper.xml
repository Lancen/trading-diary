<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 行业板块 Mapper，提供行业板块列表查询和统计 -->
<mapper namespace="com.tradingdiary.mapper.IndustryMapper">

    <!-- 查询行业板块列表，关联两融、成交占比、成分股数量，支持关键词筛选和分页排序 -->
    <select id="selectIndustryList" resultType="com.tradingdiary.collection.model.ConceptIndustryVO">
        SELECT ind.code, ind.name, ind.pinned, ind.pin_order,
               COUNT(DISTINCT sci.stock_code) AS stockCount,
               SUM(md.margin_balance) AS marginBalance, SUM(md.margin_change) AS marginChange,
               SUM(md.short_balance) AS shortBalance, SUM(md.short_change) AS shortChange,
               MAX(sci.snap_date) AS snapDate,
               CASE WHEN mkt.amount IS NOT NULL AND mkt.amount > 0
                    THEN ROUND(IFNULL(sid.amount, 0) / mkt.amount * 100, 2)
                    ELSE NULL END AS volumePct
        FROM industry ind
        LEFT JOIN stock_industry sci ON ind.code = sci.industry_code AND sci.is_deleted = 0
        LEFT JOIN margin_daily md ON sci.stock_code = md.stock_code AND md.is_deleted = 0
            <if test="tradeDate != null">AND md.trade_date = #{tradeDate}</if>
            <if test="tradeDate == null">AND md.trade_date = (SELECT MAX(trade_date) FROM margin_daily WHERE is_deleted = 0)</if>
        LEFT JOIN sector_index_daily sid ON ind.code = sid.sector_code
            AND sid.sector_type = 'INDUSTRY' AND sid.is_deleted = 0
            <if test="tradeDate != null">AND sid.trade_date = #{tradeDate}</if>
            <if test="tradeDate == null">AND sid.trade_date = (SELECT MAX(trade_date) FROM margin_daily WHERE is_deleted = 0)</if>
        LEFT JOIN (
            SELECT SUM(amount) AS amount FROM market_index_daily WHERE is_deleted = 0
            <if test="tradeDate != null">AND trade_date = #{tradeDate}</if>
            <if test="tradeDate == null">AND trade_date = (SELECT MAX(trade_date) FROM margin_daily WHERE is_deleted = 0)</if>
        ) mkt ON 1=1
        WHERE ind.is_deleted = 0
        <if test='keyword != null and keyword != ""'>AND ind.name LIKE CONCAT('%', #{keyword}, '%')</if>
        GROUP BY ind.code, ind.name, ind.pinned, ind.pin_order, mkt.amount
        ORDER BY ind.pinned DESC, ind.pin_order ASC,
        <choose>
            <when test="sortBy == 'stockCount'">stockCount</when>
            <when test="sortBy == 'marginBalance'">marginBalance</when>
            <when test="sortBy == 'marginChange'">marginChange</when>
            <when test="sortBy == 'shortBalance'">shortBalance</when>
            <when test="sortBy == 'shortChange'">shortChange</when>
            <otherwise>marginBalance</otherwise>
        </choose>
        <choose>
            <when test="sortDir == 'ASC'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计符合筛选条件的行业板块总数 -->
    <select id="countIndustryList" resultType="long">
        SELECT COUNT(DISTINCT ind.id) FROM industry ind
        LEFT JOIN stock_industry sci ON ind.code = sci.industry_code AND sci.is_deleted = 0
        LEFT JOIN margin_daily md ON sci.stock_code = md.stock_code AND md.is_deleted = 0
            <if test="tradeDate != null">AND md.trade_date = #{tradeDate}</if>
            <if test="tradeDate == null">AND md.trade_date = (SELECT MAX(trade_date) FROM margin_daily WHERE is_deleted = 0)</if>
        WHERE ind.is_deleted = 0
        <if test='keyword != null and keyword != ""'>AND ind.name LIKE CONCAT('%', #{keyword}, '%')</if>
    </select>

    <!-- 查询行业板块表的最大创建时间，用于判断采集数据新鲜度 -->
    <select id="selectMaxCreatedAt" resultType="java.time.LocalDateTime">
        SELECT MAX(created_at) FROM industry WHERE is_deleted = 0
    </select>

</mapper>
