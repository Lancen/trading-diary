<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 数据采集日志 Mapper，提供采集任务的执行记录查询 -->
<mapper namespace="com.tradingdiary.mapper.DataCollectionLogMapper">

    <!-- 查询指定数据类型和作业类型的最近一条采集日志 -->
    <select id="selectLatestByDataTypeAndJobType" resultType="com.tradingdiary.entity.DataCollectionLog">
        SELECT * FROM data_collection_log WHERE data_type = #{dataType} AND job_type = #{jobType} ORDER BY id DESC LIMIT 1
    </select>

    <!-- 查询指定数据类型的最近 N 条采集日志 -->
    <select id="selectRecentByDataType" resultType="com.tradingdiary.entity.DataCollectionLog">
        SELECT * FROM data_collection_log WHERE data_type = #{dataType} ORDER BY id DESC LIMIT #{limit}
    </select>

    <!-- 查询指定数据类型、作业类型和交易日期的最近一条采集日志 -->
    <select id="selectLatestByDataTypeAndJobTypeAndTradeDate" resultType="com.tradingdiary.entity.DataCollectionLog">
        SELECT * FROM data_collection_log WHERE data_type = #{dataType} AND job_type = #{jobType} AND trade_date = #{tradeDate} ORDER BY id DESC LIMIT 1
    </select>

    <!-- 查询最近 N 条采集日志（不限数据类型） -->
    <select id="selectRecent" resultType="com.tradingdiary.entity.DataCollectionLog">
        SELECT * FROM data_collection_log ORDER BY id DESC LIMIT #{limit}
    </select>

</mapper>
