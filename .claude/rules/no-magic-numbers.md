# 魔术数字禁止硬编码

批量大小、超时时间、重试次数等可调参数必须定义为常量（常量类或配置文件），禁止直接写数字。

**Why:** 数字散落在各处，调整时需要逐个文件搜索替换，容易遗漏。集中管理后改一处全局生效。

**How to apply:**
- 批量大小 → 常量类（如 `CollectionConstants.DB_BATCH_SIZE`）
- 超时/间隔/重试 → 常量类或 `@Value` 从配置文件注入
- 一次性的 0/1 判断值（如 `if (list.isEmpty())`）不在此列
